@using AirWeb.WebApp.Platform.PageModelHelpers
@model IEnumerable<IaipDataService.Structs.ValueWithUnits>

@if (Model.Any())
{
    @string.Join("; ", Model)
}
else
{
    @DefaultText.PlaceholderNotEntered
}
