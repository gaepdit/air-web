@using AirWeb.WebApp.Platform.PageModelHelpers
@model string?
@if (string.IsNullOrEmpty(Model))
{
    <span class="text-body-secondary fw-lighter">@SelectOptions.PlaceholderNotEntered</span>
}
else
{
    @Model
    @:County
}
