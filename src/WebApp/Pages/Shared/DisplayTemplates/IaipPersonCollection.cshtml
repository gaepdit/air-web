@model ICollection<IaipDataService.Structs.PersonName>

@if (Model is { Count: > 0 })
{
    @Model.Select(p => p.FullName).ConcatWithSeparator(", ")
    ;
}
else
{
    @DefaultText.PlaceholderNotEntered
}
