@using AirWeb.AppServices.Utilities
@using AirWeb.WebApp.Platform.PageModelHelpers
@model DateTime?
@if (Model == null)
{
    @DefaultText.NotAvailable
}
else
{
    @Model.ToString(DateTimeFormats.ShortDateTime)
}
