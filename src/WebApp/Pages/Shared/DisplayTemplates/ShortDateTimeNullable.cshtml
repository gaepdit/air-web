@using AirWeb.WebApp.Platform.Constants
@using AirWeb.WebApp.Platform.PageModelHelpers
@model DateTime?
@if (Model == null)
{
    @DefaultText.NotAvailable
}
else
{
    @Model.ToString(DateTimeFormatConstants.ShortDateTimeFormat)
}
