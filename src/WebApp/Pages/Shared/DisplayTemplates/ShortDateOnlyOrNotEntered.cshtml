@using AirWeb.WebApp.Platform.Constants
@using AirWeb.WebApp.Platform.PageModelHelpers
@model DateOnly?
@if (Model is null)
{
    <span class="text-body-secondary fw-lighter">@SelectOptions.PlaceholderNotEntered</span>
}
else
{
    @Model.Value.ToString(DateTimeFormatConstants.ShortDateFormat)
}
