@using AirWeb.WebApp.Platform.Constants
@using AirWeb.WebApp.Platform.PageModelHelpers
@model DateTime?
@if (Model == null)
{
    <span class="text-body-secondary fw-lighter">@DefaultText.NotAvailable</span>
}
else
{
    @Model.ToString(DateTimeFormatConstants.ShortDateFormat)
}
