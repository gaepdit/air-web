@model short
@{
    var addon = (string?)ViewData["Addon"] ?? string.Empty;
    var min = (int?)ViewData["Min"];
    var max = (int?)ViewData["Max"];
    var addonId = $"addon-{Guid.NewGuid()}";
}
<label asp-for="@__model" class="form-label"></label>
<div class="input-group">
    <span class="input-group-text" id="@addonId">@addon</span>
    <input asp-for="@__model" type="number" class="form-control rounded-end" aria-describedby="@addonId" min="@min" max="@max" />
    <span asp-validation-for="@__model" class="invalid-feedback ps-2"></span>
</div>
